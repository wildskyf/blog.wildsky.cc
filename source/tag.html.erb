---
pageable: true
per_page: 12
type: tag
---
<h1>標上 '<%= tagname %>' 這個 Tag 的文章有：</h1>

<% if paginate && num_pages > 1 %>
  <p>Page <%= page_number %> of <%= num_pages %></p>

  <% if prev_page %>
    <p><%= link_to 'Previous page', prev_page %></p>
  <% end %>
<% end %>

<ul>
  <% page_articles.each_with_index do |article, i| %>
    <li>
      <a href="<%= article.url %>">
        <time datetime="<%= article.data.date.strftime('%Y-%m-%d 00:00') %>"><%= article.data.date.strftime('%Y/%m/%d') %></time>
        &dash;
        <%= article.data.title %>
      </a>
    </li>
  <% end %>
</ul>

<% if paginate %>
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %>
<% end %>
